const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
const childPopulateObject = {
  path: 'times',
  select: 'id dateTime category',
};

const Constants = { monthNames, childPopulateObject };

module.exports = Constants;
